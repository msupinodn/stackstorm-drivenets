name: get_dnos_neighbors
pack: drivenets
runner_type: python-script
description: "Get DNOS LLDP neighbors"
enabled: true
entry_point: "get_lldp_neighbors.py"
parameters:
    hosts:
        type: string
        description: "dnos hostname"
    netbox_url:
        type: string
        description: "netbox_url"
    netbox_secret:
        type: string
        description: "netbox secret"

