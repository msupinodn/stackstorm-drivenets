name: get_dnos_info
pack: drivenets
runner_type: python-script
description: "Discovery and push DNOS infra to NetBox"
enabled: true
entry_point: "dnos_discovery.py"
parameters:
    hosts:
        type: string
        description: "dnos hostname"
    netbox_url:
        type: string
        description: "netbox_url"
    netbox_secret:
        type: string
        description: "netbox secret"

